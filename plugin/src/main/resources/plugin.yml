# project data for Bukkit in order to register our plugin with all it components
# ${-} are variables from Maven (pom.xml) which will be replaced after the build
name: ${project.parent.name}
version: ${project.version}
main: ${project.groupId}.${project.artifactId}.${project.parent.name}

# meta data for plugin managers
authors: [games647, 'https://github.com/games647/ClearNMS/graphs/contributors']
description: |
    ${project.parent.description}
website: ${project.parent.url}
dev-url: ${project.parent.url}

